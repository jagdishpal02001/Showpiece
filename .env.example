# Copy this file to ".env" and fill in your Supabase project values.
# Vite only exposes variables prefixed with VITE_ to the frontend.
#
# Find these in your Supabase dashboard: Project Settings -> API.
#
# The anon (public) key is SAFE to ship in the frontend: Row Level Security
# (see supabase/schema.sql) is what actually protects your data. Never put the
# service_role key here or anywhere in the client.

VITE_SUPABASE_URL=https://uamtnahhtjivhdpijrek.supabase.co/rest/v1/
VITE_SUPABASE_ANON_KEY=sb_publishable_ewGk5zCNIb72ed0OvKImOA_frFkr0bE
